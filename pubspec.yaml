name: my_tts_app
description: "High Quality AI TTS"
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^4.0.2

flutter:
  uses-material-design: true

